import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
public class SecurityConfig {

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/actuator/**").permitAll() // whitelist all actuator endpoints
                .anyRequest().authenticated()
            )
            .csrf(csrf -> csrf.disable()); // optional: disable CSRF for actuator APIs

        return http.build();
    }
}


management.endpoints.web.exposure.include=health,info,metrics,prometheus,threaddump
